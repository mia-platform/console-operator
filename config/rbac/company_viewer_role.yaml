# This rule is not used by the project console-k8s-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to core.mia-platform.eu resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: console-k8s-operator
    app.kubernetes.io/managed-by: kustomize
  name: company-viewer-role
rules:
- apiGroups:
  - core.mia-platform.eu
  resources:
  - companies
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - core.mia-platform.eu
  resources:
  - companies/status
  verbs:
  - get
